[project]
name = "navigator-backend"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pyright==1.1.361"]

[tool.uv.workspace]
members = ["backend-api"]

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["backend-api/app", "backend-api/tests"]
