name: api-services-shared
description: Shared ECS infrastructure for API microservices
runtime:
  name: python
  options:
    virtualenv: uv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
