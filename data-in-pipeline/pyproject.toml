[project]
name = "data-in-pipeline"
version = "1.0.0"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = ["prefect>=3.4", "api"]

[tool.uv.sources]
api = { workspace = true }

[dependency-groups]
dev = ["pytest>=8.4.2"]

[tool.pytest.ini_options]
pythonpath = ["."]
requires-python = ">=3.12"
dependencies = ["pydantic>=2.12.1"]
